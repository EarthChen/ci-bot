name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false
          # Fixture submodule (ci-bot-fixtures) is public; no token needed.
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Materialize fixture branches
        # actions/checkout clones the fixture submodule with a single-branch
        # fetch refspec, so class1/2/3-failing-test are missing. Force a full
        # branch fetch, then create them as LOCAL branches (the test needs them).
        run: |
          git -C fixtures/repo config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'
          git -C fixtures/repo fetch origin
          git -C fixtures/repo for-each-ref --format='%(refname:short)' refs/remotes/origin \
            | grep -v '/HEAD$' | sed 's#origin/##' \
            | while read b; do
                git -C fixtures/repo branch --force "$b" "origin/$b" 2>/dev/null || true
              done
          git -C fixtures/repo branch -a

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test
